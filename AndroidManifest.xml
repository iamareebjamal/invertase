<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="areeb.invertase"
    android:installLocation="auto"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="3"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launch"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme" >
        <activity
            android:name="areeb.invertase.Invert"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="areeb.invertase.SharerLayoutActivity"
            android:label="@string/title_activity_sharer_layout"
            android:theme="@style/Theme.Sherlock.Light.Dialog" >
        </activity>
        <activity
            android:name="areeb.invertase.InformationActivity"
            android:theme="@style/Theme.Sherlock.Light.Dialog.Title"
            android:label="Info" >
        </activity>
    </application>

</manifest>
